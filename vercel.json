{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "framework": null,
  "functions": {
    "api/index.py": {
      "runtime": "@vercel/python@4.3.1"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "//": "Python packaging note: the shared `wowanalyze` engine lives at repo root. api/index.py inserts the repo root on sys.path so it imports in the Vercel bundle. If Vercel tree-shakes it out during a real deploy, add `\"includeFiles\": \"wowanalyze/**\"` under functions."
}
