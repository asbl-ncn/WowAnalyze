# Runtime dependencies for the Vercel serverless function and the precompute job.
# Dev-only tools (uvicorn, pytest, ruff) live in pyproject.toml's [dev] extra.
fastapi>=0.110
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0
