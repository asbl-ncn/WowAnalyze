"""WowAnalyze engine — WCL client, models, diff dimensions, and reference profiles.

Shared by both the live API (`api/`) and the offline precompute (`scripts/`).
The diff logic lives here once and is never forked across languages.
"""

__version__ = "0.1.0"
